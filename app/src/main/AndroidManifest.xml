<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="GymTrack"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GymTrack">

        <activity
            android:name=".ui.RutinasActivity"
            android:exported="false" />

        <activity
            android:name=".ui.CrearRutinaActivity"
            android:exported="false" />

        <activity
            android:name=".ui.DetalleRutinaActivity"
            android:exported="false" />

        <activity
            android:name=".ui.EditarRutinaActivity"
            android:exported="false" />

        <activity
            android:name=".ui.AnadirEjercicioActivity"
            android:exported="false" />

        <activity
            android:name=".ui.MainActivity"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

    </application>

</manifest>